# 卖出流程完整总结

本文档详细说明插件在不同通道、不同筹集币种下的卖出流程。

## 📊 流程概览

```
用户点击卖出
  ↓
1. 解析代币路由 (resolveTokenRoute)
  ↓
2. 确定交易通道 (Four/Flap/Pancake)
  ↓
3. 执行卖出交易
  ↓
4. 处理筹集币种兑换（如果需要）
```

---

## 1️⃣ 代币路由解析阶段

### A. 检测代币平台

```typescript
detectTokenPlatform(tokenAddress)
  ↓
根据地址模式判断：
- 以 4444 结尾 → 'four'
- 以 0x4444 开头 → 'xmode'
- 以 7777/8888 结尾 → 'flap'
- 其他 → 'unknown'
```

### B. 查询代币状态

```typescript
fetchRouteWithFallback(platform)
  ↓
尝试顺序：
- 如果是 'unknown' → 直接查询 Pancake
- 如果是发射台 → 查询发射台合约状态
  ↓
返回：
{
  platform: 'four' | 'flap' | 'unknown',
  preferredChannel: 'four' | 'flap' | 'pancake',
  readyForPancake: boolean,  // 是否已迁移
  quoteToken: '0x...',       // 筹集币种地址
  migrating: boolean
}
```

---

## 2️⃣ 不同通道的卖出流程

### 🟢 通道 1：Four.meme（未迁移代币）

**适用场景**：
- 代币地址以 `4444` 结尾
- `liquidityAdded = false`（未迁移）
- 筹集币种：BNB 或其他

**卖出流程**：

```typescript
// 1. 准备卖出
prepareTokenSell()
  ↓ 查询代币余额
  ↓ 计算卖出数量
  ↓ 检查授权状态

// 2. 执行卖出
channelHandler.sell() → Four.meme 合约
  ↓
调用：tokenManager.sellToken(
  tokenAddress,
  amountIn,      // 卖出的代币数量
  minFunds       // 最小获得的筹集币种数量
)
  ↓
获得筹集币种（存储在合约中）

// 3. 处理筹集币种
if (筹集币种 !== BNB) {
  // 3a. 查询筹集币种余额
  getQuoteBalance()

  // 3b. 兑换筹集币种 → BNB
  executeFourQuoteSell()
    ↓
  如果筹集币种在 QUOTE_TOKEN_POOL_CONFIG 中：
    - 使用预配置的路径（V2 或 V3）
    - 例如：ASTER → WBNB (V2)
    - 例如：USD1 → WBNB (V3, fee=100)
  否则：
    - 尝试 V2 直接路径
    - 尝试 V3 多个 fee tier
    - 尝试桥接路径
}
```

**筹集币种处理示例**：

| 筹集币种 | 兑换路径 | 模式 | Fee |
|---------|---------|------|-----|
| BNB | 无需兑换 | - | - |
| ASTER | ASTER → WBNB | V2 | - |
| USD1 | USD1 → WBNB | V3 | 100 |
| USDT | USDT → WBNB | V3 | 100 |
| 其他 | 自动探测 | V2/V3 | 自动 |

---

### 🔵 通道 2：Flap（未迁移代币）

**适用场景**：
- 代币地址以 `7777` 或 `8888` 结尾
- `readyForPancake = false`（未迁移）
- 筹集币种：BNB 或其他

**卖出流程**：

```typescript
// 1. 准备卖出
prepareTokenSell()
  ↓ 查询代币余额
  ↓ 计算卖出数量
  ↓ 检查授权状态

// 2. 预估报价
getQuote(tokenAddress, ZERO_ADDRESS, amountIn)
  ↓
调用：flapPortal.quoteExactInput({
  inputToken: tokenAddress,
  outputToken: ZERO_ADDRESS,  // BNB
  inputAmount: amountIn
})
  ↓
返回预估的 BNB 数量

// 3. 执行卖出
channelHandler.sell() → Flap Portal 合约
  ↓
调用：flapPortal.swapExactInput({
  inputToken: tokenAddress,
  outputToken: ZERO_ADDRESS,  // BNB
  inputAmount: amountIn,
  minOutputAmount: minBnb,
  permitData: '0x'
})
  ↓
直接获得 BNB（无需额外兑换）
```

**特点**：
- ✅ Flap 内部自动处理筹集币种兑换
- ✅ 用户直接收到 BNB
- ✅ 无需额外的兑换步骤

---

### 🟡 通道 3：Pancake（已迁移代币）

**适用场景**：
- 代币已迁移到 Pancake
- `liquidityAdded = true` 或 `readyForPancake = true`
- 流动性池：Token ↔ QuoteToken

**卖出流程**：

```typescript
// 1. 准备卖出
prepareTokenSell()
  ↓ 查询代币余额
  ↓ 计算卖出数量

// 2. 查询最佳路由
findBestRoute('sell', tokenAddress, amountIn, quoteToken)
  ↓
// 2a. 检查缓存
if (有有效缓存 && 缓存未过期) {
  if (quoteToken && quoteToken !== WBNB) {
    // 优先尝试 QuoteToken 路径
    尝试：Token → QuoteToken → WBNB
  }
  复用缓存路径
} else {
  // 2b. 并行查询 V2 和 V3
  Promise.all([
    findBestV2Path(),  // 查询 V2 路由
    findBestV3Route()  // 查询 V3 路由
  ])
    ↓
  比较输出金额，选择最优路由
}

// 3. 检查授权
if (V2 路由) {
  检查对 PancakeRouter 的授权
} else if (V3 路由) {
  检查对 SmartRouter 的授权
}

// 4. 执行卖出
if (V2 路由) {
  调用：router.swapExactTokensForETHSupportingFeeOnTransferTokens(
    amountIn,
    amountOutMin,
    path,           // 例如：[Token, QuoteToken, WBNB]
    recipient,
    deadline
  )
} else if (V3 路由) {
  调用：smartRouter.exactInput({
    path: encodedPath,  // 编码的路径 + fees
    recipient,
    amountIn,
    amountOutMinimum
  })
  +
  smartRouter.unwrapWETH9(amountOutMin, recipient)
}
  ↓
直接获得 BNB
```

**路由选择逻辑**：

```typescript
// 优先级 1：QuoteToken 路径（如果有）
if (quoteToken && quoteToken !== WBNB) {
  尝试：Token → QuoteToken → WBNB
  if (成功) return;
}

// 优先级 2：直接路径
尝试：Token → WBNB
if (成功) return;

// 优先级 3：桥接路径
尝试：Token → USDT → WBNB
尝试：Token → USDC → WBNB
尝试：Token → BUSD → WBNB
尝试：Token → ASTER → WBNB
...
```

---

## 3️⃣ 不同筹集币种的处理

### A. BNB 筹集币种

**Four.meme**：
```
卖出代币 → 获得 BNB → 完成 ✅
```

**Flap**：
```
卖出代币 → 获得 BNB → 完成 ✅
```

**Pancake**：
```
卖出代币 → Token → WBNB → 完成 ✅
```

---

### B. 非 BNB 筹集币种（如 ASTER、USD1）

**Four.meme**：
```
卖出代币 → 获得 ASTER（存储在合约）
  ↓
查询 ASTER 余额
  ↓
兑换 ASTER → WBNB
  ↓
使用 QUOTE_TOKEN_POOL_CONFIG 中的路径：
  - ASTER: V2 直接路径
  - USD1: V3 (fee=100)
  - USDT: V3 (fee=100)
  ↓
获得 BNB → 完成 ✅
```

**Flap**：
```
卖出代币 → Flap 内部自动兑换 → 获得 BNB → 完成 ✅
```

**Pancake**：
```
卖出代币 → Token → QuoteToken → WBNB → 完成 ✅
（使用 QuoteToken 路径优化）
```

---

## 4️⃣ 性能优化点

### A. 路由缓存

```typescript
// 缓存内容
{
  lastSellPath: ['Token', 'QuoteToken', 'WBNB'],
  lastSellFees: [500],
  lastMode: 'v2',
  sellRouteLoadedAt: timestamp,
  sellRouteStatus: 'success'
}

// 缓存有效期：1 小时
// 第二次卖出：50-200ms（使用缓存）
```

### B. QuoteToken 路径优化

```typescript
// 优先尝试 QuoteToken 路径
if (quoteToken && quoteToken !== WBNB) {
  path = [Token, QuoteToken, WBNB];
  // 直接命中正确路径，避免尝试所有桥接代币
}
```

### C. 并发查询

```typescript
// 并发执行
Promise.all([
  prepareTokenSell(),      // 查询余额、授权
  findBestRoute(),         // 查询路由
  checkV2Allowance(),      // 查询 V2 授权
  checkV3Allowance()       // 查询 V3 授权
])
```

---

## 5️⃣ 流程图总结

```
用户点击卖出
  ↓
┌─────────────────────────────────────┐
│ 1. 解析代币路由                      │
│    - 检测平台（four/flap/unknown）   │
│    - 查询迁移状态                    │
│    - 获取筹集币种                    │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ 2. 选择交易通道                      │
├─────────────────────────────────────┤
│ Four.meme  │ Flap      │ Pancake    │
│ (未迁移)   │ (未迁移)  │ (已迁移)   │
└─────────────────────────────────────┘
  ↓           ↓           ↓
┌──────┐  ┌──────┐  ┌──────────┐
│卖出  │  │卖出  │  │查询路由  │
│代币  │  │代币  │  │(V2/V3)   │
└──────┘  └──────┘  └──────────┘
  ↓           ↓           ↓
┌──────┐  ┌──────┐  ┌──────────┐
│获得  │  │获得  │  │执行卖出  │
│筹集  │  │BNB   │  │          │
│币种  │  │      │  │          │
└──────┘  └──────┘  └──────────┘
  ↓           ↓           ↓
┌──────┐      │      ┌──────────┐
│兑换  │      │      │获得BNB   │
│→BNB  │      │      │          │
└──────┘      │      └──────────┘
  ↓           ↓           ↓
  └───────────┴───────────┘
              ↓
          完成 ✅
```

---

## 6️⃣ 关键代码位置

### 代币路由解析
- `src/background/index.ts` - `resolveTokenRoute()`
- `src/shared/token-route.ts` - `detectTokenPlatform()`, `fetchRouteWithFallback()`

### 通道实现
- `src/shared/trading-channels.ts` - 所有通道的 `sell()` 方法
- `src/background/four-quote-bridge.ts` - Four.meme 筹集币种兑换
- `src/background/flap-quote-agent.ts` - Flap 报价查询

### 路由查询
- `src/shared/trading-channels.ts` - `findBestRoute()`, `findBestV2Path()`, `findBestV3Route()`

### 配置
- `src/shared/trading-config.ts` - `QUOTE_TOKEN_POOL_CONFIG`（筹集币种路径配置）

---

## 7️⃣ 性能数据

| 场景 | 首次卖出 | 第二次卖出 | 优化效果 |
|------|---------|-----------|---------|
| Four.meme (BNB) | 1-2秒 | 50-200ms | 95% |
| Four.meme (非BNB) | 2-3秒 | 50-200ms | 95% |
| Flap | 1-2秒 | 50-200ms | 95% |
| Pancake (已迁移) | 3-5秒 | 50-200ms | 95% |
| Pancake (非发射台) | 1-2秒 | 50-200ms | 95% |

---

## 8️⃣ 常见问题

### Q1: 为什么 Four.meme 卖出后需要额外兑换？
**A**: Four.meme 合约卖出后获得的是筹集币种（如 ASTER、USD1），需要额外兑换成 BNB 才能给用户。

### Q2: Flap 为什么不需要额外兑换？
**A**: Flap Portal 合约内部自动处理筹集币种兑换，用户直接收到 BNB。

### Q3: Pancake 如何知道使用哪个路径？
**A**: 通过 `quoteToken` 信息，优先尝试 `Token → QuoteToken → WBNB` 路径，这是 Four.meme 已迁移代币的标准流动性池结构。

### Q4: 如何添加新的筹集币种支持？
**A**: 在 `QUOTE_TOKEN_POOL_CONFIG` 中添加配置：
```typescript
registerQuoteTokenPreset(CONTRACTS.NEW_TOKEN, {
  swapMode: 'v2' | 'v3',
  path: [CONTRACTS.WBNB, CONTRACTS.NEW_TOKEN],
  fee: 100  // 仅 V3 需要
});
```

### Q5: 为什么非发射台代币卖出更快？
**A**: 优化后，非发射台代币（地址不匹配发射台模式）直接使用 Pancake，跳过所有发射台平台探测，节省 3+ 秒。

---

## 9️⃣ 相关文档

- [交易性能优化报告](./trade-performance-optimization-plan.md)
- [RPC 请求优化报告](./rpc-request-optimization-report.md)
- [路由缓存清除指南](./clear-routing-cache.md)
- [多链抽象设计](./multi-chain-abstraction-design.md)
